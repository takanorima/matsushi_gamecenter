# 効果音ファイルの置き場所

咀嚼音(たべる音)の音源が届いたら、このフォルダに

```
chew.mp3
```

という名前で置いてください。`games/asmr/index.html` は起動時にこのファイルを読み込もうとし、
見つかればそれを再生します。まだ置かれていない間は、コードが自動生成した音(WebAudio)で代用しています。

- ファイル名は `chew.mp3` のままにしてください(コード側でこの名前を指定しているため)。
- `.wav` など別形式しか無い場合は教えてください。読み込み側のコードを合わせます。
